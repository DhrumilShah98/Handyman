</head>

<body>
	<div class="wrapper">
		<nav class="navbar navbar-expand-lg navbar-light">
			<div class="container">
				<a href="/" class="navbar-brand"><img src="/assets/images/handyman-icon.png" alt="Handyman icon"
						class="handyman-icon">HANDYMAN</a>
				<button class="navbar-toggler" data-toggle="collapse" data-target="#landingMenu">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="landingMenu">
					<ul class="navbar-nav mr-auto">
						<li class="nav-item"><a href="/about" class="nav-link">ABOUT US</a></li>
						<li class="nav-item"><a href="/contact" class="nav-link">CONTACT US</a></li>
						<li class="nav-item"><a href="/services" class="nav-link">SERVICES</a></li>
					</ul>
					<ul class="navbar-nav">
						<% if(!currentUser){ %>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="customerDropdownMenu" role="button"
								data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								CUSTOMER
							</a>
							<div class="dropdown-menu" aria-labelledby="customerDropdownMenu">
								<a class="dropdown-item" href="/customer/register">CUSTOMER REGISTERATION</a>
								<a class="dropdown-item" href="/customer/login">CUSTOMER LOGIN</a>
							</div>
						</li>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="vendorDropdownMenu" role="button"
								data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								VENDOR
							</a>
							<div class="dropdown-menu" aria-labelledby="vendorDropdownMenu">
								<a class="dropdown-item" href="/vendor/register">VENDOR REGISTERATION</a>
								<a class="dropdown-item" href="/vendor/login">VENDOR LOGIN</a>
							</div>
						</li>
						<% } else { %>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="currentUserMenu" role="button"
								data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								SIGNED IN AS <strong><%= currentUser.username %></strong>
							</a>
							<div class="dropdown-menu" aria-labelledby="currentUserMenu">
								<% if(currentUser.isCustomer){ %>
								<a class="dropdown-item" href="/customer/<%= currentUser._id %>/edit">CUSTOMER EDIT
									PROFILE</a>
								<% } %>
								<% if(!currentUser.isCustomer){ %>
								<a class="dropdown-item" href="/vendor/<%= currentUser._id %>/edit">VENDOR EDIT
									PROFILE</a>
								<a class="dropdown-item"
									href="/<%= currentUser.service %>/<%= currentUser.subservice %>/<%= currentUser._id %>">VENDOR
									VIEW PROFILE</a>
								<% } %>
							</div>
						</li>
						<li class="nav-item"><a href="/logout" class="nav-link">LOGOUT</a></li>
						<% } %>
					</ul>
				</div>
			</div>
		</nav>

		<div class="container">
			<% if(error && error.length > 0){ %>
			<div style="margin-top: 16px; font-weight: 500" class="alert alert-danger alert-dismissible" role="alert">
				<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
				<%= error %>
			</div>
			<% } %>
			<% if(success && success.length > 0){ %>
			<div style="margin-top: 16px; font-weight: 500" class="alert alert-success alert-dismissible" role="alert">
				<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
				<%= success %>
			</div>
			<% } %>
		</div>